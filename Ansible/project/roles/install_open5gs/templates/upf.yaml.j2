logger:
    file: /var/log/open5gs/{{ upf.log_filename | default ('upf.log') }}
upf:
    pfcp:
      - addr: {{ upf.pfcp_ip | default('127.0.0.7') }}
    gtpu:
      - addr: {{ upf.gtpu_ip | default('127.0.0.7') }}
    subnet:
      {% for conf in subnet_confs[subnet_slice_start:subnet_slice_end] | default([{'addr': '10.45.0.1/16', 'dnn': 'internet', 'dev': 'ogstun'}]) -%}
      - addr: {{ conf.addr }}
        dnn: {{ conf.dnn }}
        dev: {{ conf.dev }}
      {% endfor %}

    metrics:
      - addr: 127.0.0.7
        port: 9090
smf:
parameter:
max:
time:
